import React from "react";
import { Button } from "reactstrap";

import "./FirstButton.scss";

function FirstButton() {
  return (
    <div className="first">
      <Button color="primary">Player 1</Button>
    </div>
  );
}

export default FirstButton;
